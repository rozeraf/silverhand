# TODO

Актуализировано: 2026-08-28. Выполненные пункты сохранены внизу как краткая история; верхняя часть — реальный текущий бэклог.

## P0 — зависимости и воспроизводимая сборка

- [ ] Обновить `lucide-react` до версии с официальной поддержкой React 19 и проверить все используемые иконки.
- [ ] Оставить Vite и `@vitejs/plugin-react` только в `devDependencies`, убрать несовпадающие дубликаты версий из `package.json` и обновить lockfile.
- [ ] Удалить устаревший `importmap` из `index.html`, который смешивает версии React и Vite с установленными пакетами.
- [ ] Добавить скрипты `typecheck`, `check` и, после подключения линтера, `lint` в `package.json`.
- [ ] Добавить отдельный CI workflow для `bun install --frozen-lockfile`, проверки типов и сборки до merge в `main`.

## P1 — музыка и Cloudflare R2

- [ ] До публичного переноса убедиться, что распространение музыкальных файлов допустимо; подпись о правообладателях сама по себе не является лицензией.
- [ ] Создать R2 Standard bucket и публичную точку чтения (`r2.dev` на старте либо собственный домен позже).
- [ ] Настроить `Content-Type: audio/mpeg`, CORS, кеширование и проверить HTTP Range (`206 Partial Content`) для перемотки.
- [ ] Добавить переменную `VITE_MUSIC_BASE_URL` и перевести музыкальный реестр с eager `import.meta.glob` на URL R2.
- [ ] Оставить локальный fallback только для разработки либо полностью исключить `music/*.mp3` из production-бандла.
- [ ] Сохранить отображение удалённых треков в терминальной VFS без импорта бинарников в сборку.
- [ ] Проверить итоговый размер `dist/`, первый старт плеера, перемотку и повторное воспроизведение из browser cache.

## P1 — надёжность терминала

- [ ] Очищать все таймеры boot sequence при закрытии и размонтировании `Terminal`.
- [ ] Проверить повторное открытие терминала в React Strict Mode на дублирование строк и обработчиков.
- [ ] Определить, какие изменения VFS должны переживать перезагрузку, и при необходимости хранить только пользовательский overlay/diff в `localStorage`.
- [ ] Автоматически получать размер локальных медиа при сборке либо генерировать manifest, чтобы не поддерживать `size` вручную в `terminal/metadata.ts`.
- [ ] Добавить unit-тесты для нормализации путей, traversal, POSIX-прав, `chmod`, completion и login flow.
- [ ] Проверить крайние случаи shell-парсинга: лишние пробелы, кавычки, абсолютные/относительные пути и запрещённый traversal.

## P1 — надёжность плеера и медиа

- [ ] Показывать пользователю ошибки загрузки и воспроизведения аудио, а не только писать их в console.
- [ ] Проверить автоматический переход на следующий трек и поведение последнего трека.
- [ ] Обработать гонки при быстром переключении треков, повторном открытии экрана и `play()` rejection.
- [ ] Добавить fallback для повреждённых или недоступных изображений и обложек.
- [ ] Исправить расширение/формат `assets/a_like_supreme.png`: файл фактически имеет WebP media type либо должен быть перекодирован в настоящий PNG.
- [ ] Добавить изображениям ниже первого экрана `loading="lazy"`, `decoding="async"` и стабильные `width`/`height`.
- [ ] Сжать тяжёлые `malorian_arms_3516.png` и `vest.png`, сравнить качество WebP/AVIF.

## P1 — доступность

- [ ] Добавить `role="dialog"`, `aria-modal` и связанные заголовки для Terminal, Settings и Samurai overlay.
- [ ] Закрывать модальные окна по `Escape`, удерживать в них фокус и возвращать его исходной кнопке.
- [ ] Блокировать прокрутку фоновой страницы при открытом overlay.
- [ ] Добавить `aria-label` кнопкам только с иконкой и заменить кликабельные `<div>` треклиста на `<button>`.
- [ ] Добавить клавиатурное управление плеером и видимые `focus-visible` состояния.
- [ ] Проверить контраст вторичного текста и состояний hover/focus на реальных CRT-настройках.

## P2 — CRT, анимации и производительность

- [ ] Снять профиль Performance/GPU на Hero, Terminal, первой и последней секциях с включённым CRT.
- [ ] Добавить облегчённый CRT preset без SVG `feDisplacementMap` для слабых устройств.
- [ ] Останавливать WebGL loop при скрытой вкладке и статичном/неактивном эффекте.
- [ ] Учитывать `prefers-reduced-motion`; отключать glitch, scramble, tilt, parallax и постоянное мерцание.
- [ ] Сделать `enableAnimations` единым выключателем всех необязательных анимаций.
- [ ] Проверить DPR/render scale, мобильный нагрев и энергопотребление, не ухудшая геометрическую синхронизацию курсора.
- [ ] Проверить Core Web Vitals и Lighthouse на мобильном устройстве с CRT и без него.

## P2 — frontend-инфраструктура

- [ ] Перенести Tailwind CSS с CDN в локальную сборку.
- [ ] Вынести глобальные стили, keyframes и CSS-переменные из `index.html` в CSS-файлы.
- [ ] Локализовать favicon, Google Fonts и оставшиеся внешние texture URL в `index.html`, `Arsenal.tsx` и `Samurai.tsx`.
- [ ] Подключить ESLint и Prettier с единым конфигом.
- [ ] Настроить branch protection для `main`: обязательный PR из `dev`, успешный CI и запрет прямых push.
- [ ] Проверить предупреждения GitHub Actions о runtime Node и регулярно обновлять pinned major-версии actions.
- [ ] Добавить понятное сообщение/уведомление при неуспешном Cloudflare deploy.

## P2 — архитектура

- [ ] Вынести данные биографии, арсенала, треков и timeline из JSX в отдельные типизированные data-файлы.
- [ ] Разбить `Samurai.tsx` на player controller, now-playing, tracklist, timeline и members.
- [ ] Добавить Error Boundary для lazy-компонентов и экран восстановления после ошибки чанка.
- [ ] Удалить неиспользуемый `LoadingScreen` либо подключить его к реальному сценарию загрузки.
- [ ] Удалить динамически собранные Tailwind-классы либо safelist-ить их после перехода на локальную сборку.
- [ ] Определить URL/history-стратегию для Samurai screen, терминала и открытых разделов.
- [ ] Добавить тесты для `SettingsContext`, таймера/переключения плеера и classified unlock.

## P3 — SEO и контент

- [ ] Добавить `meta description`, Open Graph, Twitter Card, canonical URL и локальное social preview.
- [ ] Добавить `robots.txt` и sitemap, если сайт должен индексироваться.
- [ ] Проверить структуру заголовков `h1`–`h4` и доступные названия всех секций.
- [ ] Проверить даты, имена и формулировки по лору Cyberpunk с источниками.
- [ ] Унифицировать русский и английский язык интерфейса и исправить оставшиеся опечатки.
- [ ] Сделать логотип Navbar ссылкой на начало и показывать активный раздел.
- [ ] Проверить мобильную вёрстку на ширинах 320–400 px и работу на медленном соединении.
- [ ] Добавить в репозиторий явный `LICENSE` для собственного кода и отдельный `NOTICE` для сторонних материалов.

## Выполнено

- [x] Рабочий процесс упрощён до веток `dev` и `main`; `prod` удалена.
- [x] Cloudflare Pages deploy запускается только по тегу `v*` или опубликованному GitHub Release.
- [x] Workflow использует Bun и Wrangler 4, не устанавливая Wrangler через конфликтующий npm dependency tree.
- [x] Все пять треков и обложек подключены к плееру локально.
- [x] Таймер плеера показывает минуты, секунды и десятые доли секунды.
- [x] Внешние ссылки из `assets.ts` заменены локальными ассетами; добавлены Relic, бронежилет и обновлённые изображения.
- [x] Текстовые документы терминала вынесены из TypeScript в реальные файлы `terminal/files/`.
- [x] VFS автоматически строится из текстов, изображений и MP3 через `import.meta.glob`.
- [x] Реализованы owner/group/mode и проверки `rwx`, команды `id`, `groups`, `tree`, `stat`, файловые операции и авторизация.
- [x] Добавлены `Tab` completion и навигация по истории стрелками вверх/вниз.
- [x] Добавлена терминальная загадка через публичные `target.txt` и `incident.txt`; секрет не раскрывается в документации.
- [x] CRT-эффект вынесен в отдельную настройку `Experimental` с предупреждением о высокой нагрузке.
- [x] На сайте и в README добавлены уведомления о правах CD PROJEKT S.A., Refused и других правообладателей.
